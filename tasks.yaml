version: v0.0.1
tasks:
  - name: hello-gopher
    runner: busybox
    command: ["cp", "tasks.yaml", "output.yaml"]
    cleanup: true
  - name: gopher
    runner: busybox
    command: ["echo","output.yaml", ">>", "/output.yaml"]
    cleanup: true
