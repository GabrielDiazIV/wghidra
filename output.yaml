version: v0.0.1
tasks:
  - name: hello-gopher
    runner: busybox
    command: ["cp", "tasks.yaml", "output.yaml"]
    cleanup: true
  - name: gopher
    runner: busybox
    command: ["touch", "output.yaml"]
    cleanup: true
    cleanup: true
    command: ["tac", "tasks.yaml", " >>", "output.yaml"]
    runner: busybox
  - name: gopher
    cleanup: true
    command: ["cp", "tasks.yaml", "output.yaml"]
    runner: busybox
  - name: hello-gopher
tasks:
version: v0.0.1
